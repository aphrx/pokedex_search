import { IconButton, InputBase } from '@material-ui/core';

import styled from 'styled-components';

export const SearchCameraButton = styled(IconButton)`
    padding: 10px;
`;

export const SearchInput = styled(InputBase)`
    padding-left: 10px;
    margin-left: 1;
    flex: 1;
`